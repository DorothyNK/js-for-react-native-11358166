# js-for-react-native-11358166

NAME: KOTEY DOROTHY NAA-DEI
ID: 11358166
DCIT 202 ASSIGNMENT ONE



processArray()
In the arrayManipulation file, a function processArray takes an array of numbers as an argument,, the number is squared if the element of the array is even multiplied by 3 if the number is odd.

formatArrayString()
A function formatArrayString takes in two arguments,thus an array of strings and the array from the processArray function. If the modified array of numbers from the process function is even, the function returns an Uppercase string in the string array. If the modified array of numbers from the process function is odd, the function returns an lowercase string in the string array.

createUserProfile
The modified string is exported and imported in the userInfo.js file. A function createUserProfile takes in two strings as an argument names and the imported modified string. The function returns an array of objects containing a name and an ID starting from 1 and automatically there is an increment by 1.